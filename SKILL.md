---
name: format-note
description: 将 md 笔记格式化为项目统一样式。输入文件路径格式化单个文件，输入目录路径格式化整个目录下的 md 文件。
---

# 笔记格式规范

对目标 md 文件（或目录下所有 md 文件），按以下规则格式化。

## 标题层级

```
# 章节标题

## 大节标题

### 小节标题

#### 小小节标题
```

所有标题（`#` `##` `###` `####`）**前后各空一行**。

## Q&A 格式

```
1. Q: 问题文本
	1. 答案项1
	2. 答案项2
		- 子项子项

2. Q: 下一个问题
	1. 答案项
```

规则：
- 编号后**一个空格**（`1. xxx`，不是 `1.  xxx`）
- 编号：**全文件连续**，不按节重置
- Q 行以 `Q: ` 开头（`1. Q: 某某某的含义`）
- Q 行和 A 行之间：**无空行**
- A 行是 Q 的**有序子项**，用 Tab 缩进 + `1. ` `2. ` 编号
- 答案下的子项是**无序子项**，用双 Tab 缩进 + `- `
- 两个 Q&A 对之间：**空一行**

**例外：表格 / 代码块前后必须空行**。当答案用表格（`| ... |`）、代码块（```` ``` ````）或 mermaid 图呈现时，即便它紧跟在 `Q: ` 行之后，其前后仍需各空一行（这是 Markdown 渲染要求），此时「Q 行和 A 行之间无空行」不适用。

## 强调

- `**粗体**`：**尊重原文**，仅保留原文已有的加粗。**不要主动**给术语、概念加粗。
- HTML 标签内（如 `<small>` 中）的 `<b>` / `<strong>` 等**保留原样**，不要替换成 `**...**`——HTML 内嵌套 Markdown 会导致渲染出错（应为 `<small>…<b>…</b>…</small>`，而非 `<small>…**…**…</small>`）。
- `<small>...</small>`：备注说明（如 `<small>就业是最大的民生</small>`、`<small>不用记</small>`）

## 链接

### 内部链接（Obsidian 指向仓库文档的 `[[...]]`）

- 别名（`|` 后面的显示文本）**内容不做改动**，仅在其**前面加一个 🔗**
- 有别名：`[[1_领悟人生真谛，把握人生方向#^f6d89c|人生观的内容]]` → `[[1_领悟人生真谛，把握人生方向#^f6d89c|🔗人生观的内容]]`
- 无别名（`[[目标]]`）：保持原样

### 外部链接（`[文字](url)`）

- **保留原文**，不做任何改动

## 块引用 `>`

前后各空一行。分两类，用粗体标签区分：

### 口诀

```
> **口诀**：超、实、代 → 超越性、实践性、时代性
```

多行口诀：

```
> **口诀**：
> 中华民族站富强
> 科社主义高举旗
> 保持独立供选择
```

### 概念辨析

```
> **辨析**：信念和信仰不一定是短期内可实现的，而理想是。
```

## 缩进

- 一级缩进用 **Tab**（答案项）
- 二级缩进用 **Tab + Tab**（答案的子项）

## 避免

- 编号后**不要**双空格
- Q 和 A 之间**不要**空行
- **不要**用 `？` 结尾 Q 行（`Q: ` 前缀已标明问题）

## 格式化流程

1. 读取目标文件
2. 识别结构：标题层级、Q&A 对、口诀/辨析块引用
3. 按上述规则调整：
   - 标题前后补空行
   - Q 行加 `Q: ` 前缀、编号全文件连续
   - A 行转为有序缩进列表，子项转为无序缩进列表
   - 块引用加 `**口诀**：` 或 `**辨析**：` 标签
   - 内部链接别名前加 🔗，外部链接保持原样
4. 写回文件
